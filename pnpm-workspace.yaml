packages:
  - 'packages/*'
  - 'examples/*'
# testbed/ stays outside the workspace on purpose: it is docker and documentation,
# with no package of its own to build.

# pnpm 10: packages allowed to run their postinstall scripts.
onlyBuiltDependencies:
  - '@biomejs/biome'
  - esbuild
  - turbo
